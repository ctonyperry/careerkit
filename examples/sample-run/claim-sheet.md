# Claim sheet - Halcyon Robotics Solutions Engineer

## Coverage

Five of six required rows HIT. `degree` is a credential carrying "or
equivalent experience" and gets no bullet. `infra` is preferred and DECLINED:
Morgan has never run infrastructure and the cover letter says so.

## The bound that shapes this package

`lantern-security-questionnaires` carries a bound: Morgan coordinated SOC 2
evidence and never ran the audit. The security bullet renders as coordination.
`lantern-scim-rate-limit` carries a doubted contract figure that is never
printed; the user count carries the scale.

## Angle

The posting's own thread is the integration owner who stays from the security
review through go-live. Morgan held that seat at Lantern and the resume
evidences each phase the JD names in order: identity rollout, debugging from
logs, data contracts, security review.

## Bullets to evidence

| Section | Claim | Evidence |
|---|---|---|
| Lantern | SSO and SCIM rollouts for 40 enterprise tenants on Okta and Entra ID | `lantern-sso-rollouts` |
| Lantern | SCIM failure traced to rate limiting from logs, pipeline fixed, sync re-run | `lantern-scim-rate-limit` |
| Lantern | security questionnaires and SOC 2 evidence coordination; escalation point | `lantern-security-questionnaires` |
| Lantern | reporting export redesigned; data contract written and signed | `lantern-reporting` |
| Brightline | LMS integrations mapping SCORM completion data into compliance reports | `brightline-lms-integrations` |
| Brightline | runbook and debugging guide for support triage | `brightline-runbook` |
| Northwind | EDI integration support, incidents tracked in Jira | `northwind-edi-support` |
| Earlier | help desk line | `helpdesk-early` |

## Excluded on purpose

- `brightline-enablement` is provisional. The JD prefers enablement experience
  and `brightline-runbook` covers it with a confirmed unit, so the workshops
  wait for Morgan's confirmation.
- `project-webhook-replayer` is a personal project and this resume has no
  Projects section.
